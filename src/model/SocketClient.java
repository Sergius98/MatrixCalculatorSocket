package model;

public class SocketClient {
    // receive from server word "begin[20]" where 20 is length of a line in matrix
    // receive "A:/20 ints/B:/20 ints/
    // stop when receive "stop", otherwise print error
}
